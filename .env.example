# Node
NODE_ENV

# AuthJS
AUTH_GOOGLE_ID
AUTH_GOOGLE_SECRET
# The following secret can be generated with the command `openssl rand -base64 64`
AUTH_SECRET

# Database
DATABASE_CONNECTION_STR

# Email service
EMAIL_SERVICE_API_KEY
# The following secret can be generated with the command `openssl rand -base64 64`
EMAIL_SERVICE_SECRET
